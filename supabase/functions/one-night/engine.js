// Generated by scripts/build-one-night-edge.mjs. Edit api/src/one-night instead.
'use strict';
import { randomUUID, randomInt } from 'node:crypto';
import coreRoles from './core-roles.json' with {type:'json'};
import extraRoles from './expansion-roles.json' with {type:'json'};
import * as expansion from './expansions.js';
const catalogue = [...coreRoles, ...extraRoles];
const roleMap = new Map(catalogue.map(r => [r.id, r]));
const WOLVES = new Set(['werewolf', 'alphaWolf', 'mysticWolf', 'dreamWolf']);
const VAMPIRES = new Set(['vampire', 'master', 'count']);
const DEFAULT_SETTINGS = { discussionSeconds: 300, loneWolf: true };
const BOT_ACTION_DELAY = 1500;
const ARTIFACTS = ['claw', 'brand', 'cudgel', 'void', 'mask', 'shroud', ...Object.keys(expansion.BONUS_ARTIFACTS || {})];
const COPY_LATE = new Set(['insomniac', 'revealer', 'curator', 'count', 'renfield', 'priest', 'assassin', 'apprenticeAssassin', 'marksman', 'pickpocket', 'gremlin', 'auraSeer', 'squire', 'beholder', 'apprenticeTanner', 'groob', 'zerb', 'oracle', 'leader', 'psychic', 'rascal', 'exposer', 'blob', 'mortician', 'empath', 'nostradamus']);
const NO_ACTION = new Set(catalogue.filter(r => r.order == null).map(r => r.id));
const clone = value => JSON.parse(JSON.stringify(value));
const nowMs = value => Number.isFinite(value) ? value : Date.now();
const text = (en, zh) => ({ en, zh });
const nameOf = (id, lang = 'en') => roleMap.get(id)?.name?.[lang] || id;
function fail(code, message) { const e = new Error(message); e.code = code; e.clientSafe = true; throw e; }
function cleanName(value) { const result = typeof value === 'string' ? value.trim().replace(/\s+/g, ' ') : ''; if (!result || result.length > 30) fail('INVALID_NAME', 'Enter a name with 1–30 characters.'); return result; }
function phase(room, kind, step = kind, now = Date.now()) { room.phase = { id: randomUUID(), kind, step, cueIds: [], deadline: null }; if (kind === 'discussion') room.phase.deadline = now + room.settings.discussionSeconds * 1000; }
function event(room, en, zh, now) { room.events.push({ id: randomUUID(), text: text(en, zh), at: now }); room.events = room.events.slice(-60); }
function learn(room, seat, en, zh) { seat.knowledge.push({ id: randomUUID(), text: text(en, zh) }); }
function makeSeat(name, actorId) { return { id: randomUUID(), name, actorId: actorId || null, photo: null, ready: false, originalRoleId: null, nightRoleId: null, knowledge: [] }; }
function seatOf(room, actor) { return room.seats.find(s => s.id === room.members[actor]?.seatId && s.actorId === actor) || null; }
function getSeat(room, id) { const seat = room.seats.find(s => s.id === id); if (!seat) fail('INVALID_TARGET', 'Choose a player in this room.'); return seat; }
function host(room, actor) { if (!room.hostId || actor !== room.hostId) fail('HOST_ONLY', 'Only the room host can do that.'); }
function checkPhase(room, command, kinds) { if (!kinds.includes(room.phase.kind)) fail('WRONG_PHASE', 'This action is not available now.'); const expected = command.expectedPhaseId || command.phaseId; if (expected && expected !== room.phase.id) fail('STALE_PHASE', 'The phase changed; refresh and try again.'); }
function connected(room, s, now) { return Boolean(s.actorId && room.members[s.actorId] && (s.isBot || now - room.members[s.actorId].lastSeen < 35000)); }
function createRoom({ code, hostId, hostName, now }) { now = nowMs(now); if (!hostId) fail('INVALID_HOST', 'A host session is required.'); const seat = makeSeat(cleanName(hostName), hostId); const room = { version: 1, code, revision: 1, createdAt: now, updatedAt: now, hostId, status: 'lobby', members: { [hostId]: { seatId: seat.id, lastSeen: now } }, seats: [seat], requests: [], settings: { ...DEFAULT_SETTINGS }, botState: { mode: 'automatic', nextActionAt: 0 }, roleDeck: [], phase: null, events: [], cards: {}, marks: {}, artifacts: {}, shields: [], revealed: [], actions: {}, nightState: {}, actionLog: [], votes: {}, result: null, expansion: {} }; phase(room, 'lobby', 'lobby', now); event(room, 'A new One Night room is ready.', '一夜狼人杀房间已创建。', now); return room; }
function validateDeck(deck, n) {
    if (!Array.isArray(deck) || deck.length !== n + 3 || deck.some(id => !roleMap.has(id))) fail('INVALID_DECK', 'Choose exactly three more role cards than players.');
    for (const role of catalogue) { const count = deck.filter(id => id === role.id).length; if (count > (role.maxCount || 1)) fail('INVALID_DECK', `Too many ${role.name.en} cards.`); }
    const masons = deck.filter(id => id === 'mason').length; if (masons === 1) fail('INVALID_DECK', 'Use both Mason cards or neither.');
    if (n < 3 || n > 16) fail('PLAYER_COUNT', 'One Night supports 3–16 players in this room.');
}
function defaultDeck(n) { return ['werewolf', 'werewolf', 'seer', 'robber', 'troublemaker', ...['villager', 'villager', 'villager', 'drunk', 'insomniac', 'tanner', 'hunter', 'minion', 'sentinel', 'apprenticeSeer', 'witch', 'revealer', 'curator', 'bodyguard'].slice(0, n - 2)]; }
function shuffle(list) { const out = [...list]; for (let i = out.length - 1; i > 0; i--) { const j = randomInt(i + 1); [out[i], out[j]] = [out[j], out[i]]; } return out; }
function cardRole(card) { if (!card) return 'villager'; return card.transformedRoleId || card.copiedRoleId || card.roleId; }
function effective(room, id) {
    const card = room.cards[id]; let roleId = cardRole(card); let team = roleMap.get(roleId)?.team || 'village';
    const artifact = room.artifacts[id];
    const artifactRoles = { claw: 'werewolf', brand: 'villager', cudgel: 'tanner', clawOfTheWerewolf: 'werewolf', brandOfTheVillager: 'villager', cudgelOfTheTanner: 'tanner' };
    if (artifactRoles[artifact]) { roleId = artifactRoles[artifact]; team = roleMap.get(roleId)?.team || 'village'; }
    else if (room.marks[id] === 'vampire') team = 'vampire';
    if (['copycat', 'doppelganger'].includes(roleId)) team = 'village';
    const value = { roleId, team };
    return expansion.effective ? expansion.effective(makeCtx(room, null, 'effective'), id, value) : value;
}
function originalActors(room, role) { return room.seats.filter(s => s.nightRoleId === role).map(s => s.id); }
function nightActorRole(room, seat, step) {
    if (step.startsWith('doppel:')) return seat.copyMode === 'doppel' && seat.nightRoleId === step.slice(7) ? step.slice(7) : null;
    if (step === 'markReview') return 'markReview';
    if (step === 'lovers') return seat.duskMark === 'love' ? 'lovers' : null;
    if (seat.copyMode === 'copycatPassive') return null;
    if (seat.copyMode === 'doppel' && !['werewolf', 'vampire', 'mason', 'alien'].includes(step)) return step === 'doppelganger' ? (seat.doppelImmediate || null) : null;
    if (step === 'werewolf') return WOLVES.has(seat.nightRoleId) ? (seat.nightRoleId === 'dreamWolf' ? 'dreamWolf' : 'werewolf') : null;
    if (step === 'vampire') return VAMPIRES.has(seat.nightRoleId) ? 'vampire' : null;
    if (seat.nightRoleId === step) return step;
    if (expansion.getActors || expansion.actors) { const ids = (expansion.getActors || expansion.actors)(makeCtx(room, seat, step), step); if (ids?.includes(seat.id)) return step; }
    return null;
}
function currentData(room, seat, role) { const key = `${room.phase.step}:${seat.id}`; return room.nightState[key] ||= { role, count: 0, seen: [] }; }
function completed(room, seat) { return Boolean(room.actions[room.phase.step]?.[seat.id]); }
function complete(room, seat) { (room.actions[room.phase.step] ||= {})[seat.id] = true; }
function makeCtx(room, seat, role) {
    const players = (includeSelf = true) => room.seats.filter(s => includeSelf || s.id !== seat?.id).map(s => s.id);
    const centers = () => Object.keys(room.cards).filter(id => id.startsWith('center:'));
    const label = id => id.startsWith('center:') ? text(`Center ${Number(id.split(':')[1]) + 1}`, `中央牌 ${Number(id.split(':')[1]) + 1}`) : text(`#${getSeat(room, id).number} ${getSeat(room, id).name}`, `${getSeat(room, id).number} 号 ${getSeat(room, id).name}`);
    const record = (type, ids) => { room.actionLog.push({ seatId: seat.id, roleId: role, type, targets: [...ids] }); room.nightState.viewedOrMoved ||= []; if (!room.nightState.viewedOrMoved.includes(seat.id)) room.nightState.viewedOrMoved.push(seat.id); };
    const inspectCard = id => { if (!room.cards[id] || room.shields.includes(id)) fail('SHIELDED', 'That card cannot be viewed.'); const card = clone(room.cards[id]); record('view', [id]); learn(room, seat, `${label(id).en}: ${nameOf(card.roleId)}.`, `${label(id).zh}：${nameOf(card.roleId, 'zh')}。`); return card; };
    const swapCards = (a, b) => { if (!room.cards[a] || !room.cards[b] || a === b || room.shields.includes(a) || room.shields.includes(b)) fail('INVALID_TARGET', 'Choose two different unshielded cards.'); [room.cards[a], room.cards[b]] = [room.cards[b], room.cards[a]]; record('move', [a, b]); };
    return { room, seat, role, roles: catalogue, step: room.phase.step, data: seat ? currentData(room, seat, role) : {}, seats: room.seats, players, centers, targets: ids => ids.map(id => ({ id, label: label(id) })), inspectCard, swapCards, inspectMark: id => { getSeat(room, id); const mark = room.marks[id] || 'clarity'; learn(room, seat, `${label(id).en}: ${mark}.`, `${label(id).zh}：${markName(mark, 'zh')}。`); return mark; }, swapMarks: (a, b) => { getSeat(room, a); getSeat(room, b); if (a === b) fail('INVALID_TARGET', 'Choose two different players.'); [room.marks[a], room.marks[b]] = [room.marks[b], room.marks[a]]; }, learn: (en, zh) => learn(room, seat, en, zh), effectiveCard: id => effective(room, id), originalActors: r => originalActors(room, r), complete: () => complete(room, seat), fail, randomInt, actionLog: room.actionLog, label };
}
function markName(mark, locale) { const names = { clarity: ['Clarity', '清醒'], vampire: ['Vampire', '吸血鬼'], fear: ['Fear', '恐惧'], bat: ['Bat', '蝙蝠'], disease: ['Disease', '疾病'], love: ['Love', '爱情'], traitor: ['Traitor', '叛徒'], assassin: ['Assassin', '刺客'] }; return names[mark]?.[locale === 'zh' ? 1 : 0] || mark; }
function schedule(room) {
    const roles = new Set(room.roleDeck); const turns = [];
    for (const id of roles) {
        const info = roleMap.get(id); if (info.order == null || ['master', 'dreamWolf', 'syntheticAlien'].includes(id)) continue;
        turns.push({ roleId: id, step: id, order: info.order });
    }
    if ([...roles].some(id => WOLVES.has(id)) && !turns.some(t => t.step === 'werewolf')) turns.push({ roleId: 'werewolf', step: 'werewolf', order: 2 });
    if ([...roles].some(id => VAMPIRES.has(id)) && !turns.some(t => t.step === 'vampire')) turns.push({ roleId: 'vampire', step: 'vampire', order: -6 });
    if ([...roles].some(id => roleMap.get(id)?.expansion === 'vampire')) { turns.push({ roleId: 'markReview', step: 'markReview', order: -.3 }); if (roles.has('cupid')) turns.push({ roleId: 'lovers', step: 'lovers', order: -.2 }); }
    if (roles.has('doppelganger') || roles.has('mirrorMan')) for (const id of roles) if (COPY_LATE.has(id)) turns.push({ roleId: id, step: `doppel:${id}`, order: (roleMap.get(id)?.order || 0) + .01 });
    if (expansion.augmentSchedule) { const extra = expansion.augmentSchedule(makeCtx(room, null, 'schedule')); if (Array.isArray(extra)) turns.push(...extra); }
    return turns.sort((a, b) => a.order - b.order).filter((t, i, a) => a.findIndex(x => x.step === t.step) === i);
}
function beginTurn(room, now) {
    const turn = room.schedule[room.nightIndex];
    if (!turn) return startDiscussion(room, now);
    phase(room, 'night', turn.step, now); room.phase.roleId = turn.roleId; room.phase.nightStage = 'opening'; room.phase.cueIds = [...(room.nightIndex === 0 ? ['night'] : []), `role-${turn.roleId}`]; room.phase.deadline = now + 30000;
}
function beginActing(room, now) {
    room.phase.id = randomUUID(); room.phase.nightStage = 'acting'; room.phase.cueIds = []; room.phase.deadline = null;
    room.nightActors = room.seats.filter(s => nightActorRole(room, s, room.phase.step) && !(room.phase.step !== 'markReview' && (room.schedule[room.nightIndex].order >= 0 || room.phase.step === 'lovers') && s.duskMark === 'fear')).map(s => s.id);
    if (!room.nightActors.length) room.phase.deadline = now + randomInt(7000, 15001);
    for (const id of room.nightActors) initializeAction(room, getSeat(room, id), now);
}
function finishActing(room, now) { room.phase.id = randomUUID(); room.phase.nightStage = 'closing'; room.phase.cueIds = ['close']; room.phase.deadline = now + 12000; }
function nextTurn(room, now) { room.nightIndex++; beginTurn(room, now); }
function initializeAction(room, seat) {
    const role = nightActorRole(room, seat, room.phase.step); const data = currentData(room, seat, role); if (data.initialized) return; data.initialized = true;
    const ctx = makeCtx(room, seat, role); const peers = ids => ids.filter(id => id !== seat.id).map(id => `#${getSeat(room, id).number}`).join(', ') || '—';
    if (role === 'werewolf') { const wolves = room.seats.filter(s => WOLVES.has(s.nightRoleId) && s.duskMark !== 'fear' && s.copyMode !== 'copycatPassive').map(s => s.id); ctx.learn(`Original wolves: ${peers(wolves)}.`, `起始狼人：${peers(wolves)}。`); }
    if (['minion', 'renfield', 'mason', 'vampire'].includes(role)) { const ids = room.seats.filter(s => role === 'minion' ? WOLVES.has(s.nightRoleId) : role === 'mason' ? s.nightRoleId === 'mason' : VAMPIRES.has(s.nightRoleId)).map(s => s.id); ctx.learn(`${nameOf(role)} contacts: ${peers(ids)}.`, `${nameOf(role, 'zh')}确认对象：${peers(ids)}。`); if (role === 'renfield') { const infected = room.seats.filter(s => room.marks[s.id] === 'vampire').map(s => s.id); ctx.learn(`Vampire mark: ${peers(infected)}.`, `吸血鬼标记：${peers(infected)}。`); room.marks[seat.id] = 'bat'; } }
    if (role === 'lovers') ctx.learn(`Love marks at dusk: ${peers(room.seats.filter(s => s.duskMark === 'love').map(s => s.id))}.`, `黄昏时的恋人：${peers(room.seats.filter(s => s.duskMark === 'love').map(s => s.id))}。`);
    if (role === 'markReview') { seat.duskMark = room.marks[seat.id] || 'clarity'; ctx.inspectMark(seat.id); }
    if (role === 'priest') room.marks[seat.id] = 'clarity';
    expansion.initialize?.(ctx);
    if (role === 'apprenticeAssassin') { data.assassins = originalActors(room, 'assassin').filter(id => getSeat(room, id).copyMode !== 'doppel'); for (const id of data.assassins) learn(room, getSeat(room, id), `Apprentice Assassin: #${seat.number}.`, `见习刺客：${seat.number} 号。`); if (data.assassins.length) ctx.learn(`Assassin: ${peers(data.assassins)}.`, `刺客：${peers(data.assassins)}。`); }
}
function descriptor(room, seat) {
    if (room.status !== 'playing' || room.phase.kind !== 'night' || room.phase.nightStage !== 'acting' || !room.nightActors?.includes(seat.id) || completed(room, seat)) return null;
    const role = nightActorRole(room, seat, room.phase.step); const ctx = makeCtx(room, seat, role), data = ctx.data;
    const others = ctx.players(false), all = ctx.players(), cards = ids => ids.filter(id => !room.shields.includes(id));
    const base = (en, zh, ids = [], min = 0, max = min, canSkip = false, options) => ({ id: `${room.phase.id}:${seat.id}:${data.count}`, roleId: role, prompt: text(en, zh), targets: ctx.targets(ids), min, max, canSkip, ...(options ? { options } : {}) });
    const choices = values => values.map(([id, en, zh]) => ({ id, label: text(en, zh) }));
    if (['minion', 'mason', 'renfield', 'markReview', 'lovers'].includes(role)) return base('Read your private information, then continue.', '请阅读私密信息，然后继续。');
    if (role === 'dreamWolf') return base('Confirm your part of the shared wolves turn. Dream Wolf does not learn teammates or inspect cards. Tap Continue, then close your eyes.', '与其他狼人同时确认本阶段。梦狼不会获知狼队友，也不能查牌。点击继续后请闭眼。');
    if (role === 'werewolf') { const awake = room.seats.filter(s => WOLVES.has(s.nightRoleId) && s.duskMark !== 'fear' && s.copyMode !== 'copycatPassive').length; return base(awake === 1 && room.settings.loneWolf ? 'You may inspect one center card, or continue without looking.' : 'Recognize your fellow wolves, then continue.', awake === 1 && room.settings.loneWolf ? '你可以查看一张中央牌，或直接继续。' : '确认狼队友后继续。', awake === 1 && room.settings.loneWolf ? ctx.centers() : [], 0, awake === 1 && room.settings.loneWolf ? 1 : 0, false); }
    if (['doppelganger', 'copycat'].includes(role)) return base('Choose a card to copy.', '选择要复制的身份牌。', role === 'copycat' ? ctx.centers() : cards(others), 1, 1);
    if (role === 'seer' && !data.mode) return base('Choose how to inspect cards.', '选择查验方式。', [], 0, 0, true, choices([['player', 'One other player', '一位其他玩家'], ['center', 'Two center cards', '两张中央牌']]));
    if (['seer', 'mysticWolf', 'apprenticeSeer', 'paranormalInvestigator'].includes(role)) { const ids = role === 'seer' ? data.mode === 'center' ? ctx.centers() : cards(others) : role === 'apprenticeSeer' ? ctx.centers() : cards(others).filter(id => !data.seen.includes(id)); const count = role === 'seer' && data.mode === 'center' ? 2 : 1; return base(role === 'seer' ? data.mode === 'center' ? 'Inspect two center cards.' : 'Inspect one other player.' : role === 'paranormalInvestigator' ? 'Inspect another player. Stop after at most two cards, or immediately if transformed.' : 'Choose a card to inspect.', role === 'seer' ? data.mode === 'center' ? '查看两张中央牌。' : '查看一位其他玩家。' : role === 'paranormalInvestigator' ? '依次查看其他玩家，最多两张；变身后立即停止。' : '选择要查看的牌。', ids, count, count, true); }
    if (role === 'robber') return base('House rule: exchange your card with another player’s or a center card, then view only the card you receive. Shielded cards cannot move.', '房规：将自己的牌与另一位玩家的牌或一张中央牌交换，仅查看换入的牌。受保护的牌不可移动。', room.shields.includes(seat.id) ? [] : cards([...others, ...ctx.centers()]), 1, 1, true);
    if (role === 'troublemaker') return base('House rule: exchange two different unshielded cards other than your own. Choose other players’ or center cards; do not look at either.', '房规：交换自己以外任意两张不同且未受保护的牌，可选择其他玩家的牌或中央牌；不得查看。', cards([...others, ...ctx.centers()]), 2, 2, true);
    if (role === 'drunk') return base(room.shields.includes(seat.id) ? 'Your card is shielded; continue without exchanging.' : 'Exchange with one center card without looking.', room.shields.includes(seat.id) ? '你的牌受到保护，无法交换，请继续。' : '与一张中央牌交换，不查看新牌。', room.shields.includes(seat.id) ? [] : ctx.centers(), room.shields.includes(seat.id) ? 0 : 1);
    if (role === 'insomniac') return base('Check your current card (unless shielded), then continue.', '查看自己的当前牌（受保护则不可查看），然后继续。');
    if (role === 'sentinel') return base('Shield one other player’s card.', '保护一位其他玩家的身份牌。', cards(others), 1, 1, true);
    if (role === 'alphaWolf') { const ids = cards(others).filter(id => seat.copyMode === 'doppel' && room.phase.step === 'doppelganger' ? id !== seat.copySourceId : !WOLVES.has(getSeat(room, id).nightRoleId)); return base(ids.length ? 'Exchange the extra center wolf with a non-pack player.' : 'No unshielded non-pack target is available; continue.', ids.length ? '将额外中央狼人牌与一位非狼队友交换。' : '没有可交换的未受保护目标，请继续。', ids, ids.length ? 1 : 0); }
    if (role === 'witch') return data.center ? base('You must now give the inspected center card to a player.', '必须将刚查看的中央牌与一位玩家交换。', cards(all), 1, 1) : base('Inspect one center card; afterward you must exchange it.', '查看一张中央牌；查看后必须交换。', ctx.centers(), 1, 1, true);
    if (role === 'villageIdiot') return base('Rotate movable cards, skipping your card and shields.', '移动其他未受保护的玩家牌，跳过自己和受保护的牌。', [], 0, 0, true, choices([['clockwise', 'Clockwise', '顺时针'], ['counterclockwise', 'Counterclockwise', '逆时针']]));
    if (role === 'revealer') return base('Inspect and reveal another card unless it is a wolf or Tanner.', '查看并公开另一位玩家的牌，狼人或皮匠除外。', cards(others).filter(id => !room.revealed.includes(id)), 1, 1, true);
    if (role === 'curator') return base('Give a random artifact to a player.', '给一位玩家随机神器。', cards(all).filter(id => !room.artifacts[id]), 1, 1, true);
    if (role === 'vampire') {
        const ids = all.filter(id => !VAMPIRES.has(getSeat(room, id).nightRoleId));
        const votes = room.nightActors.map(id => ({ seatId: id, targetId: currentData(room, getSeat(room, id), role).target || null }));
        const summary = votes.map(vote => vote.targetId ? `#${getSeat(room, vote.seatId).number} → #${getSeat(room, vote.targetId).number}` : `#${getSeat(room, vote.seatId).number} → …`).join('; ');
        return { ...base(`Agree on one player to receive the Vampire mark. ${summary}`, `共同选定吸血鬼标记对象。${summary}`, ids, ids.length ? 1 : 0), votes };
    }
    if (role === 'count') { const ids = all.filter(id => !VAMPIRES.has(getSeat(room, id).nightRoleId) && room.marks[id] !== 'vampire'); return base('Give Fear to a non-vampire.', '给非吸血鬼玩家恐惧标记。', ids, ids.length ? 1 : 0); }
    if (role === 'diseased') { const i = room.seats.indexOf(seat), n = room.seats.length; return base('Give Disease to a neighboring player.', '给相邻玩家疾病标记。', [room.seats[(i + n - 1) % n].id, room.seats[(i + 1) % n].id], 1, 1); }
    if (role === 'cupid') return base('Give up to two Love marks.', '给予最多两枚爱情标记。', all, 0, 2, true);
    if (role === 'instigator') return base('Give a Traitor mark to any player.', '给任意玩家叛徒标记。', all, 1, 1, true);
    if (role === 'priest') return base('Your mark is now Clarity. You may clear one other player’s mark.', '你的标记已变为清醒，也可以清除另一位玩家的标记。', others, 0, 1);
    if (role === 'assassin' || role === 'apprenticeAssassin' && !data.assassins?.length) return base('Give an Assassin mark to any player.', '给任意玩家刺客标记。', all, 1, 1);
    if (role === 'apprenticeAssassin') return base('Remember the Assassin, then continue.', '记住刺客身份，然后继续。');
    if (role === 'marksman') return data.cardDone ? base('You may inspect a different player’s mark.', '可查看另一位不同玩家的标记。', others.filter(id => id !== data.cardTarget), 0, 1) : base('You may inspect another player’s card; then inspect a different mark.', '可先查看另一位玩家的牌，再查看不同玩家的标记。', cards(others), 0, 1);
    if (role === 'pickpocket') return base('Exchange your mark with another player and view the new mark.', '与另一位玩家交换标记并查看自己的新标记。', others, 1, 1, true);
    if (role === 'gremlin' && !data.mode) return base('Choose whether to exchange cards or marks.', '选择交换身份牌或标记。', [], 0, 0, true, choices([['cards', 'Cards', '身份牌'], ['marks', 'Marks', '标记']]));
    if (role === 'gremlin') return base(data.mode === 'cards' ? 'Exchange two unshielded player cards.' : 'Exchange two player marks. Card shields do not protect marks.', data.mode === 'cards' ? '交换两位未受保护玩家的身份牌。' : '交换两位玩家的标记。护盾不会保护标记。', data.mode === 'cards' ? cards(all) : all, 2, 2, true);
    const extra = expansion.buildAction?.(ctx, role); if (extra) return { ...extra, id: base('', '').id, roleId: role, ...(role === 'empath' ? { questionId: room.expansion.empathQuestion } : {}) };
    if (NO_ACTION.has(role)) return base('This role has no night action. Continue.', '此身份没有夜间行动，请继续。');
    fail('UNSUPPORTED_ROLE', `The ${role} action is unavailable.`);
}
function copyRole(room, seat, targetCard, mode) {
    const own = room.cards[seat.id]; const looked = targetCard.roleId; const copied = looked === 'copycat' || looked === 'doppelganger' || looked === 'mirrorMan' ? cardRole(targetCard) : looked;
    own.copiedRoleId = copied; seat.nightRoleId = copied; seat.copyMode = mode;
    if (mode === 'doppel' && looked === 'copycat') learn(room, seat, 'You inherit the Copycat’s chosen identity without learning it or taking its night action.', '你继承模仿者复制的身份，但不会得知该身份，也不执行其夜间行动。');
    else learn(room, seat, `Copied role: ${nameOf(copied)}.`, `复制的身份：${nameOf(copied, 'zh')}。`);
    if (mode === 'doppel') {
        if (looked === 'copycat') { seat.copyMode = 'copycatPassive'; complete(room, seat); return; }
        if (NO_ACTION.has(copied) || COPY_LATE.has(copied) || ['mason', ...VAMPIRES, 'werewolf', ...(expansion.ALIENS || []).filter(id => id !== 'bodySnatcher')].includes(copied)) { complete(room, seat); return; }
        seat.doppelImmediate = copied; const data = currentData(room, seat, copied); data.initialized = false; initializeAction(room, seat);
    } else complete(room, seat);
}
function perform(room, seat, command) {
    let action = descriptor(room, seat); if (!action) fail('NO_ACTION', 'You have no action in this phase.');
    if (command.actionId !== action.id) fail('STALE_ACTION', 'Your action changed; refresh and try again.');
    const targets = command.targets || []; if (!Array.isArray(targets) || new Set(targets).size !== targets.length) fail('INVALID_TARGET', 'Choose distinct targets.');
    // Accept an already-open pre-upgrade Gremlin form, while new clients choose
    // a mode first so its target list never offers shielded cards.
    if (action.roleId === 'gremlin' && !currentData(room, seat, 'gremlin').mode && targets.length === 2 && ['cards', 'marks'].includes(command.choice)) { currentData(room, seat, 'gremlin').mode = command.choice; action = descriptor(room, seat); }
    if (command.skip) { if (!action.canSkip) fail('MANDATORY_ACTION', 'This action must be completed.'); if (extraRoles.some(r => r.id === action.roleId) && expansion.perform?.(makeCtx(room, seat, action.roleId), action.roleId, command)) return; complete(room, seat); return; }
    if (targets.length < action.min || targets.length > action.max || targets.some(id => !action.targets.some(t => t.id === id))) fail('INVALID_TARGET', 'Choose the permitted number of valid targets.');
    if (action.options?.length && !action.options.some(o => o.id === command.choice)) fail('INVALID_CHOICE', 'Choose one of the available actions.');
    const role = action.roleId, ctx = makeCtx(room, seat, role), data = ctx.data, [a, b] = targets; data.count++;
    if (['doppelganger', 'copycat'].includes(role)) { seat.copySourceId = a; copyRole(room, seat, ctx.inspectCard(a), role === 'doppelganger' ? 'doppel' : 'copycat'); return; }
    if (role === 'seer' && !data.mode) { data.mode = command.choice; return; }
    if (role === 'seer') { if (!(targets.length === 1 && !a.startsWith('center:') || targets.length === 2 && targets.every(id => id.startsWith('center:')))) fail('INVALID_TARGET', 'Inspect one other player or exactly two center cards.'); targets.forEach(ctx.inspectCard); }
    else if (['mysticWolf', 'apprenticeSeer'].includes(role)) ctx.inspectCard(a);
    else if (role === 'paranormalInvestigator') { const card = ctx.inspectCard(a); data.seen.push(a); if (WOLVES.has(card.roleId) || card.roleId === 'tanner') { room.cards[seat.id].transformedRoleId = WOLVES.has(card.roleId) ? 'werewolf' : 'tanner'; ctx.learn(`You became ${nameOf(room.cards[seat.id].transformedRoleId)}.`, `你已变为${nameOf(room.cards[seat.id].transformedRoleId, 'zh')}。`); } else if (data.seen.length < 2 && descriptor(room, seat).targets.length) return; }
    else if (role === 'werewolf') { if (a) ctx.inspectCard(a); }
    else if (role === 'robber') { ctx.swapCards(seat.id, a); ctx.inspectCard(seat.id); }
    else if (role === 'troublemaker') ctx.swapCards(a, b);
    else if (role === 'drunk') { if (a) ctx.swapCards(seat.id, a); }
    else if (role === 'insomniac') { if (!room.shields.includes(seat.id)) ctx.inspectCard(seat.id); }
    else if (role === 'sentinel') room.shields.push(a);
    else if (role === 'alphaWolf') { if (a) ctx.swapCards('center:3', a); }
    else if (role === 'witch') { if (!data.center) { ctx.inspectCard(a); data.center = a; return; } ctx.swapCards(data.center, a); }
    else if (role === 'villageIdiot') { const ids = ctx.players(false).filter(id => !room.shields.includes(id)); if (ids.length > 1) { const cards = ids.map(id => room.cards[id]); ids.forEach((id, i) => { const offset = command.choice === 'clockwise' ? ids.length - 1 : 1; room.cards[id] = cards[(i + offset) % ids.length]; }); room.actionLog.push({ seatId: seat.id, roleId: role, type: 'move', targets: ids, direction: command.choice }); room.nightState.viewedOrMoved ||= []; if (!room.nightState.viewedOrMoved.includes(seat.id)) room.nightState.viewedOrMoved.push(seat.id); } }
    else if (role === 'revealer') { const card = ctx.inspectCard(a); if (!WOLVES.has(card.roleId) && card.roleId !== 'tanner') room.revealed.push(a); }
    else if (role === 'curator') { const pool = room.settings.artifacts?.length ? room.settings.artifacts : ARTIFACTS; const available = pool.filter(x => !Object.values(room.artifacts).includes(x)); if (available.length) room.artifacts[a] = available[randomInt(available.length)]; }
    else if (role === 'vampire') { if (a) { data.target = a; const votes = room.nightActors.map(id => currentData(room, getSeat(room, id), role).target); if (!votes.every(v => v && v === a)) return; room.marks[a] = 'vampire'; } room.nightActors.forEach(id => complete(room, getSeat(room, id))); }
    else if (role === 'count') { if (a) room.marks[a] = 'fear'; }
    else if (role === 'diseased') room.marks[a] = 'disease';
    else if (role === 'cupid') targets.forEach(id => { room.marks[id] = 'love'; });
    else if (role === 'instigator') room.marks[a] = 'traitor';
    else if (role === 'priest') { if (a) room.marks[a] = 'clarity'; }
    else if (role === 'assassin' || role === 'apprenticeAssassin' && !data.assassins?.length) room.marks[a] = 'assassin';
    else if (role === 'marksman') { if (!data.cardDone) { if (a) ctx.inspectCard(a); data.cardDone = true; data.cardTarget = a; return; } if (a) ctx.inspectMark(a); }
    else if (role === 'pickpocket') { ctx.swapMarks(seat.id, a); ctx.inspectMark(seat.id); }
    else if (role === 'gremlin') { if (!data.mode) { data.mode = command.choice; return; } if (data.mode === 'cards') ctx.swapCards(a, b); else ctx.swapMarks(a, b); }
    else if (!['minion', 'mason', 'renfield', 'markReview', 'lovers', 'apprenticeAssassin'].includes(role) && !NO_ACTION.has(role)) { if (!expansion.perform?.(ctx, role, command)) fail('UNSUPPORTED_ROLE', `The ${role} action is unavailable.`); return; }
    complete(room, seat);
}
const ARTIFACT_GUIDANCE = {
    claw: ['Claw of the Werewolf. Your role is now Werewolf; join the wolf team.', '狼人之爪。你的身份变为狼人，加入狼人阵营。'],
    brand: ['Brand of the Villager. Your role is now Villager, with no special ability.', '村民烙印。你的身份变为普通村民，没有特殊技能。'],
    cudgel: ['Cudgel of the Tanner. Your role is now Tanner. Your goal is to be eliminated.', '皮匠之棍。你的身份变为皮匠，以自己出局为获胜目标。'],
    void: ['Void of Nothingness. Nothing changes; keep the role and powers of your final card.', '虚无之境。没有额外效果，按最终身份牌及其能力结算。'],
    mask: ['Mask of Muting. Stay silent during discussion; gestures are allowed.', '禁言面具。讨论时不能说话，可以用手势交流。'],
    shroud: ['Shroud of Shame. Face away from the table. You may speak, but cannot look at players, cards or tokens, even while voting.', '羞耻之布。背对牌桌，可以说话，但不能看其他玩家、身份牌或标记，投票时也不能转身。'],
    bowOfHunter: ['Bow of the Hunter. Your role is now Hunter. If you die, the player you voted for also dies.', '猎人之弓。你的身份变为猎人。若你死亡，你投票指向的玩家也会死亡。'],
    cloakOfPrince: ['Cloak of the Prince. Your role is now Prince. Votes cannot eliminate you, but other death effects still apply.', '王子斗篷。你的身份变为王子，不会被投票放逐，但仍可能被其他死亡效果带走。'],
    swordOfBodyguard: ['Sword of the Bodyguard. Your role is now Bodyguard. Your vote protects its target instead of counting against them.', '保镖之剑。你的身份变为保镖。你投票指向的人受到保护，该票不计入其得票数。'],
    mistOfVampire: ['Mist of the Vampire. Your role is now Vampire; join the vampire team.', '吸血鬼迷雾。你的身份变为吸血鬼，加入吸血鬼阵营。'],
    alienArtifact: ['Alien Artifact. Your role is now Alien; join the alien team.', '外星神器。你的身份变为外星人，加入外星人阵营。'],
    daggerOfTraitor: ['Dagger of the Traitor. To win, another player on your final team must die.', '叛徒匕首。要获胜，必须有另一位与你最终阵营相同的玩家死亡。'],
};
function startDiscussion(room, now) {
    phase(room, 'discussion', 'discussion', now); room.phase.cueIds = ['discussion'];
    for (const seat of room.seats) {
        const artifact = room.artifacts[seat.id];
        if (!artifact) continue;
        const guidance = ARTIFACT_GUIDANCE[artifact];
        const changesRole = ['claw', 'brand', 'cudgel'].includes(artifact) || Boolean(expansion.BONUS_ARTIFACTS?.[artifact]?.roleId);
        const suffix = changesRole ? [' This replaces your card’s previous role and end-of-game powers.', '这会替换原身份及其终局能力。'] : ['', ''];
        learn(room, seat, `Your artifact: ${guidance?.[0] || 'Check the artifact rules.'}${suffix[0]}`, `你的神器：${guidance?.[1] || '请查看神器规则。'}${suffix[1]}`);
    }
    event(room, 'The night is over. Discuss what changed, without viewing your current card.', '天亮了。讨论夜里发生的变化，但不能查看当前身份。', now);
}
function resetRound(room, now) {
    room.status = 'lobby'; room.gameId = null; room.cards = {}; room.marks = {}; room.artifacts = {}; room.shields = []; room.revealed = []; room.actions = {}; room.nightState = {}; room.actionLog = []; room.votes = {}; room.result = null; room.expansion = {}; room.events = [];
    room.schedule = []; room.nightActors = []; room.nightIndex = 0;
    if (room.botState) room.botState.nextActionAt = now + BOT_ACTION_DELAY;
    room.seats.forEach(seat => { seat.ready = false; seat.originalRoleId = null; seat.nightRoleId = null; seat.originalCardId = null; seat.knowledge = []; delete seat.copyMode; delete seat.copySourceId; delete seat.doppelImmediate; delete seat.duskMark; });
    phase(room, 'lobby', 'lobby', now);
}
function start(room, now) {
    const deck = room.roleDeck.length ? room.roleDeck : defaultDeck(room.seats.length); validateDeck(deck, room.seats.length); if (room.seats.some(s => !s.actorId)) fail('EMPTY_SEATS', 'Every seat must have a player before dealing.');
    room.roleDeck = [...deck]; const shuffled = shuffle(deck); room.gameId = randomUUID(); room.status = 'playing'; room.cards = {}; room.marks = {}; room.artifacts = {}; room.shields = []; room.revealed = []; room.actions = {}; room.nightState = {}; room.actionLog = []; room.votes = {}; room.result = null; room.expansion = {};
    if (room.botState) room.botState.nextActionAt = now + BOT_ACTION_DELAY;
    room.seats.forEach((seat, i) => { const roleId = shuffled[i]; seat.number = i + 1; seat.originalRoleId = roleId; seat.nightRoleId = roleId; seat.originalCardId = null; seat.ready = false; seat.knowledge = []; delete seat.copyMode; delete seat.copySourceId; delete seat.doppelImmediate; delete seat.duskMark; room.cards[seat.id] = { id: randomUUID(), roleId }; seat.originalCardId = room.cards[seat.id].id; room.marks[seat.id] = 'clarity'; });
    shuffled.slice(room.seats.length).forEach((roleId, i) => { room.cards[`center:${i}`] = { id: randomUUID(), roleId }; }); if (deck.includes('alphaWolf')) room.cards['center:3'] = { id: randomUUID(), roleId: 'werewolf' };
    phase(room, 'ready', 'ready', now); room.phase.cueIds = ['ready']; expansion.prepare?.(makeCtx(room, null, 'prepare')); room.schedule = schedule(room); room.nightIndex = 0; event(room, 'Read your role card and ready up.', '请查看身份牌并确认准备。', now);
}
function finishVotes(room, now) {
    const result = { effective: Object.fromEntries(room.seats.map(s => [s.id, effective(room, s.id)])), votes: { ...room.votes }, counts: {}, dead: new Set(), winners: new Set(), protected: new Set() };
    const ctx = makeCtx(room, null, 'vote'); expansion.beforeVote?.(ctx, result);
    const e = result.effective, ids = room.seats.map(s => s.id), team = id => e[id].team;
    const actualWolves = ids.filter(id => team(id) === 'wolf' && WOLVES.has(e[id].roleId));
    const roleArtifact = id => ['claw', 'brand', 'cudgel'].includes(room.artifacts[id]) || Boolean(expansion.BONUS_ARTIFACTS?.[room.artifacts[id]]?.roleId);
    const actualVampires = ids.filter(id => team(id) === 'vampire' && (VAMPIRES.has(e[id].roleId) || room.marks[id] === 'vampire' && !roleArtifact(id)));
    const actualAliens = ids.filter(id => team(id) === 'alien' && ((expansion.ALIENS || []).includes(e[id].roleId) || room.cards[id]?.alien || room.expansion?.playerAliens?.includes(id)));
    const actualVillains = ids.filter(id => team(id) === 'villain' && (expansion.VILLAINS || []).includes(e[id].roleId));
    const creatures = { wolf: actualWolves, vampire: actualVampires, alien: actualAliens, villain: actualVillains };
    for (const id of ids) { if (e[id].roleId === 'renfield' && !actualVampires.length) e[id].team = 'village'; if (e[id].roleId === 'assassin' && !Object.values(room.marks).includes('assassin')) e[id].team = 'village'; }
    for (const [voter, target] of Object.entries(result.votes)) { if (['bodyguard', 'defenderEr'].includes(e[voter].roleId)) result.protected.add(target); else result.counts[target] = (result.counts[target] || 0) + 1; if (e[target].roleId === 'master' && actualVampires.includes(voter)) result.protected.add(target); }
    const hostileTeams = ['wolf', 'vampire', 'alien', 'villain'].filter(t => creatures[t].length);
    const activeFactions = hostileTeams.length + (ids.some(id => team(id) === 'village') ? 1 : 0); const epic = activeFactions >= 3;
    const eligible = ids.filter(id => !result.protected.has(id)); const max = Math.max(0, ...eligible.map(id => result.counts[id] || 0));
    if (max >= (epic ? 1 : 2)) {
        const top = eligible.filter(id => result.counts[id] === max); top.forEach(id => result.dead.add(id));
        if (epic && top.length === 1) { const second = Math.max(0, ...eligible.filter(id => !top.includes(id)).map(id => result.counts[id] || 0)); if (second > 0) eligible.filter(id => result.counts[id] === second).forEach(id => result.dead.add(id)); }
    }
    expansion.replaceDeaths?.(ctx, result);
    let changed = true;
    while (changed) { const before = result.dead.size; for (const id of [...result.dead]) if (e[id].roleId === 'hunter' && result.votes[id] && !result.protected.has(result.votes[id])) result.dead.add(result.votes[id]); const lovers = ids.filter(id => room.marks[id] === 'love'); if (lovers.some(id => result.dead.has(id))) lovers.forEach(id => result.dead.add(id)); changed = result.dead.size > before; }
    const dead = result.dead; const tannerDied = ids.some(id => e[id].roleId === 'tanner' && team(id) === 'tanner' && dead.has(id)); const hostileDied = t => creatures[t].some(id => dead.has(id));
    const villageWin = epic ? hostileTeams.filter(hostileDied).length >= 2 : hostileTeams.length ? hostileTeams.some(hostileDied) : !dead.size;
    for (const id of ids) {
        const role = e[id].roleId, t = team(id); let won = false;
        if (t === 'village') won = villageWin;
        else if (t === 'wolf') won = actualWolves.length ? !tannerDied && !hostileDied('wolf') && (!epic || hostileTeams.some(x => x !== 'wolf' && hostileDied(x))) : ['minion', 'squire'].includes(role) && [...dead].some(x => x !== id) && !tannerDied;
        else if (['vampire', 'alien', 'villain'].includes(t)) won = !hostileDied(t) && (!epic || hostileTeams.some(x => x !== t && hostileDied(x)));
        else if (t === 'tanner') won = dead.has(id);
        else if (role === 'assassin') won = ids.some(x => room.marks[x] === 'assassin' && dead.has(x));
        else if (role === 'apprenticeAssassin') { const assassins = ids.filter(x => e[x].roleId === 'assassin'); won = assassins.length ? assassins.some(x => dead.has(x)) : ids.some(x => room.marks[x] === 'assassin' && dead.has(x)); }
        if (won) result.winners.add(id);
    }
    expansion.afterVote?.(ctx, result); if (!expansion.afterVote) expansion.resolve?.(ctx, result);
    for (const id of ids) {
        if (room.marks[id] === 'traitor') { const mates = ids.filter(other => other !== id && team(other) === team(id)); if (mates.length) { result.winners.delete(id); if (mates.some(other => dead.has(other))) result.winners.add(id); } }
        const target = result.votes[id]; if (target && (e[target].roleId === 'diseased' || room.marks[target] === 'disease')) result.winners.delete(id);
    }
    room.result = { deaths: [...dead], winners: [...result.winners], counts: result.counts, votes: result.votes, epic, players: ids.map(seatId => ({ seatId, ...e[seatId], originalRoleId: getSeat(room, seatId).originalRoleId, mark: room.marks[seatId], artifact: room.artifacts[seatId] || null, won: result.winners.has(seatId), died: dead.has(seatId) })), center: Object.entries(room.cards).filter(([id]) => id.startsWith('center:')).map(([id, card]) => ({ id, roleId: cardRole(card) })), timeline: clone(room.actionLog) };
    room.status = 'finished'; phase(room, 'finished', 'finished', now); const winningTeams = [...new Set(room.result.players.filter(player => player.won).map(player => player.team))];
    room.phase.cueIds = [...(winningTeams.length ? winningTeams.map(team => `victory-${team}`) : ['victory-none']), ...room.seats.flatMap((seat, index) => result.winners.has(seat.id) ? [`winner-seat-${index + 1}`] : [])]; event(room, 'Reveal every card. The result uses your final card, mark and artifact.', '公布所有身份。胜负依据最终身份、标记和神器结算。', now);
}
function replace(room, seat, actor, name, now) { if (seat.actorId) delete room.members[seat.actorId]; seat.actorId = actor; seat.name = name; seat.isBot = false; if (room.phase.kind === 'ready') seat.ready = false; room.members[actor] = { seatId: seat.id, lastSeen: now }; }
function execute(room, actor, command, now) {
    if (!command || typeof command.type !== 'string') fail('INVALID_COMMAND', 'Choose a valid action.');
    if (room.status === 'disbanded') fail('ROOM_CLOSED', 'This room has been disbanded.');
    const seat = seatOf(room, actor); if (room.members[actor]) room.members[actor].lastSeen = now;
    switch (command.type) {
        case 'addBots': {
            host(room, actor); checkPhase(room, command, ['lobby']);
            if (!Number.isInteger(command.count) || command.count < 1 || command.count > 15) fail('INVALID_BOT_COUNT', 'Choose 1–15 test bots.');
            if (room.seats.length + command.count > 16) fail('ROOM_FULL', 'One Night rooms support at most 16 players.');
            for (let index = 0; index < command.count; index++) {
                let number = 1;
                while (room.seats.some(player => player.name === `Test Bot ${number}`)) number++;
                const bot = makeSeat(`Test Bot ${number}`, `bot:${randomUUID()}`);
                bot.isBot = true;
                room.seats.push(bot);
                room.members[bot.actorId] = { seatId: bot.id, lastSeen: now };
            }
            room.botState ||= { mode: 'automatic', nextActionAt: 0 };
            event(room, `${command.count} test bots joined the lobby.`, `${command.count} 个测试机器人加入大厅。`, now);
            return;
        }
        case 'removeBots': {
            host(room, actor); checkPhase(room, command, ['lobby']);
            const bots = room.seats.filter(player => player.isBot);
            for (const bot of bots) delete room.members[bot.actorId];
            room.seats = room.seats.filter(player => !player.isBot);
            if (bots.length) event(room, 'The host removed the test bots.', '房主已移除测试机器人。', now);
            return;
        }
        case 'setBotMode':
            host(room, actor);
            if (!['lobby', 'playing'].includes(room.status)) fail('WRONG_PHASE', 'Bot controls are available before or during a game.');
            if (command.expectedPhaseId !== room.phase.id) fail('STALE_PHASE', 'The phase changed; refresh and try again.');
            if (!['automatic', 'manual'].includes(command.mode)) fail('INVALID_BOT_MODE', 'Choose automatic or manual bot control.');
            room.botState = { mode: command.mode, nextActionAt: now + BOT_ACTION_DELAY };
            return;
        case 'botStep':
            host(room, actor);
            if (command.expectedPhaseId !== room.phase.id) fail('STALE_PHASE', 'The phase changed; refresh and try again.');
            checkPhase(room, command, ['ready', 'night', 'discussion', 'voting']);
            if (room.phase.paused) fail('PAUSED', 'The game is paused.');
            if (room.status !== 'playing') fail('WRONG_PHASE', 'Start a game before stepping test bots.');
            if (!performBotDecision(room, now)) fail('BOT_IDLE', 'No test bot needs to act now. Complete the human decision or use the host phase controls.');
            return;
        case 'heartbeat': if (!room.members[actor] && !room.requests.some(r => r.actorId === actor)) fail('NOT_SEATED', 'Request to join this room.'); return;
        case 'requestJoin': { const name = cleanName(command.name); if (seat) return; if (room.status === 'lobby') { const vacant = room.seats.find(s => !s.actorId && s.name === name); if (vacant) replace(room, vacant, actor, name, now); else { if (room.seats.length >= 16) fail('ROOM_FULL', 'This room is full.'); const added = makeSeat(name, actor); room.seats.push(added); room.members[actor] = { seatId: added.id, lastSeen: now }; } } else { room.requests = room.requests.filter(r => r.actorId !== actor); room.requests.push({ id: randomUUID(), actorId: actor, name, createdAt: now }); } return; }
        case 'approveJoin': { host(room, actor); const req = room.requests.find(r => r.id === command.requestId); if (!req) fail('INVALID_REQUEST', 'That join request no longer exists.'); const id = command.replaceSeatId || command.seatId; if (id) { const target = getSeat(room, id); if (target.actorId === room.hostId) fail('HOST_SEAT', 'Transfer host control before replacing the host.'); replace(room, target, req.actorId, req.name, now); } else { checkPhase(room, command, ['lobby']); if (room.seats.length >= 16) fail('ROOM_FULL', 'This room is full.'); const added = makeSeat(req.name, req.actorId); room.seats.push(added); room.members[req.actorId] = { seatId: added.id, lastSeen: now }; } room.requests = room.requests.filter(r => r.id !== req.id); return; }
        case 'rejectJoin': host(room, actor); room.requests = room.requests.filter(r => r.id !== command.requestId); return;
        case 'leave': if (!seat) { room.requests = room.requests.filter(r => r.actorId !== actor); return; } if (actor === room.hostId) fail('HOST_LEAVE', 'Transfer host control or disband the room.'); delete room.members[actor]; if (room.status === 'lobby') room.seats = room.seats.filter(s => s.id !== seat.id); else { seat.actorId = null; seat.isBot = false; } return;
        case 'addSeat': host(room, actor); checkPhase(room, command, ['lobby']); if (room.seats.length >= 16) fail('ROOM_FULL', 'This room is full.'); room.seats.push(makeSeat(cleanName(command.name), null)); return;
        case 'removeSeat': { host(room, actor); checkPhase(room, command, ['lobby']); const target = getSeat(room, command.seatId); if (target.actorId === room.hostId) fail('HOST_SEAT', 'You cannot remove the host.'); delete room.members[target.actorId]; room.seats = room.seats.filter(s => s.id !== target.id); return; }
        case 'moveSeat': {
            host(room, actor); checkPhase(room, command, ['lobby']);
            if (!Number.isInteger(command.number) || command.number < 1 || command.number > room.seats.length) fail('INVALID_SEATS', 'Choose a valid seat number.');
            const target = getSeat(room, command.seatId);
            room.seats.splice(room.seats.indexOf(target), 1); room.seats.splice(command.number - 1, 0, target);
            return;
        }
        case 'reorderSeats': { host(room, actor); checkPhase(room, command, ['lobby']); const ids = command.seatIds; if (!Array.isArray(ids) || ids.length !== room.seats.length || new Set(ids).size !== ids.length || ids.some(id => !room.seats.some(s => s.id === id))) fail('INVALID_SEATS', 'Include each seat exactly once.'); room.seats = ids.map(id => getSeat(room, id)); return; }
        case 'profile': case 'updateProfile': if (!seat) fail('NOT_SEATED', 'Join a seat first.'); if (command.name != null) seat.name = cleanName(command.name); if (command.photo != null) { if (typeof command.photo !== 'string' || command.photo.length > 200) fail('INVALID_PHOTO', 'Choose a valid profile image.'); seat.photo = command.photo; } return;
        case 'configure': host(room, actor); checkPhase(room, command, ['lobby']); if (command.roleDeck) { if (!Array.isArray(command.roleDeck) || command.roleDeck.length > 19 || command.roleDeck.some(id => !roleMap.has(id))) fail('INVALID_DECK', 'Choose valid role cards.'); room.roleDeck = [...command.roleDeck]; } if (command.settings) { if (command.settings.discussionSeconds != null) { const value = Number(command.settings.discussionSeconds); if (!Number.isInteger(value) || value < 30 || value > 1800) fail('INVALID_SETTINGS', 'Discussion time must be 30–1800 seconds.'); room.settings.discussionSeconds = value; } if (command.settings.loneWolf != null) room.settings.loneWolf = Boolean(command.settings.loneWolf); if (command.settings.artifacts != null) { const artifacts = command.settings.artifacts; if (!Array.isArray(artifacts) || !artifacts.length || artifacts.some(x => !ARTIFACTS.includes(x)) || new Set(artifacts).size !== artifacts.length) fail('INVALID_SETTINGS', 'Choose valid unique artifacts.'); room.settings.artifacts = artifacts; } if (command.settings.alienVariant != null) { if (!['recognize', 'center', 'convert', 'random'].includes(command.settings.alienVariant)) fail('INVALID_SETTINGS', 'Choose a supported alien variant.'); room.settings.alienVariant = command.settings.alienVariant; } } return;
        case 'start': host(room, actor); checkPhase(room, command, ['lobby']); start(room, now); return;
        case 'ready': checkPhase(room, command, ['ready']); if (!seat) fail('NOT_SEATED', 'Join a seat first.'); seat.ready = true; return;
        case 'startNight': host(room, actor); checkPhase(room, command, ['ready']); if (!room.seats.every(s => s.ready)) fail('NOT_READY', 'Every player must read their card and ready up.'); beginTurn(room, now); return;
        case 'narrationDone': if (!(command.expectedPhaseId || command.phaseId)) fail('STALE_PHASE', 'A phase id is required.'); host(room, actor); checkPhase(room, command, ['night']); if (room.phase.nightStage === 'opening') beginActing(room, now); else if (room.phase.nightStage === 'closing') nextTurn(room, now); return;
        case 'act': checkPhase(room, command, ['night']); if (!seat) fail('NOT_SEATED', 'Join a seat first.'); perform(room, seat, command); if (room.nightActors.length && room.nightActors.every(id => completed(room, getSeat(room, id)))) finishActing(room, now); return;
        case 'hardSkip': if (!(command.expectedPhaseId || command.phaseId)) fail('STALE_PHASE', 'A phase id is required.'); host(room, actor); checkPhase(room, command, ['night']); if (room.phase.nightStage === 'opening') beginActing(room, now); else if (room.phase.nightStage === 'closing') nextTurn(room, now); else { room.nightActors.forEach(id => complete(room, getSeat(room, id))); finishActing(room, now); event(room, 'The host skipped the current night action.', '房主已跳过当前夜间行动。', now); } return;
        case 'startVote': host(room, actor); checkPhase(room, command, ['discussion']); phase(room, 'voting', 'voting', now); room.phase.cueIds = ['voting']; room.votes = {}; return;
        case 'vote': checkPhase(room, command, ['voting']); if (!seat) fail('NOT_SEATED', 'Join a seat first.'); getSeat(room, command.targetId); if (command.targetId === seat.id) fail('SELF_VOTE', 'Vote for another player.'); room.votes[seat.id] = command.targetId; return;
        case 'finishVote': host(room, actor); checkPhase(room, command, ['voting']); if (!room.seats.every(s => room.votes[s.id])) fail('PENDING_VOTES', 'Every player must vote before results.'); finishVotes(room, now); return;
        case 'restartRound':
            host(room, actor);
            if (command.expectedPhaseId !== room.phase.id) fail('STALE_PHASE', 'The phase changed; refresh and try again.');
            checkPhase(room, command, ['ready', 'night', 'discussion', 'voting', 'finished']);
            resetRound(room, now);
            event(room, 'The host restarted the round. Review the setup and deal new cards.', '房主已重新开始本局。请确认配置后重新发牌。', now);
            return;
        case 'rematch': host(room, actor); checkPhase(room, command, ['finished']); resetRound(room, now); return;
        case 'transferHost': { host(room, actor); const target = getSeat(room, command.seatId); if (!target.actorId || target.isBot) fail('EMPTY_SEAT', 'Choose an occupied human seat.'); room.hostId = target.actorId; return; }
        case 'disband': case 'disbandRoom': host(room, actor); room.status = 'disbanded'; room.hostId = null; room.requests = []; room.members = {}; room.seats.forEach(s => { s.actorId = null; s.isBot = false; }); room.cards = {}; room.marks = {}; room.artifacts = {}; room.actions = {}; room.result = null; phase(room, 'finished', 'disbanded', now); return;
        default: fail('INVALID_COMMAND', 'Unknown room action.');
    }
}
function applyCommand(room, actor, command, now) { now = nowMs(now); if (command?.type === 'heartbeat' && room.members[actor] && now - room.members[actor].lastSeen < 10000) return room; const working = clone(room); execute(working, actor, command, now); working.revision++; working.updatedAt = now; Object.keys(room).forEach(k => delete room[k]); Object.assign(room, working); return room; }
// Deliberately takes the same private projection as a player, never the room.
// These are simple legal test decisions, not an attempt at strategic play.
function chooseBotCommand(view) {
    const { me, phase } = view;
    if (!me || view.status !== 'playing' || phase.paused || !view.seats.find(seat => seat.id === me.seatId)?.isBot) return null;
    const command = { expectedPhaseId: phase.id };
    if (phase.kind === 'ready') return me.ready ? null : { ...command, type: 'ready' };
    const offset = value => [...value].reduce((hash, char) => (Math.imul(hash, 31) + char.charCodeAt(0)) >>> 0, 0);
    if (phase.kind === 'voting') {
        const targets = view.seats.filter(seat => seat.id !== me.seatId);
        return me.vote || !targets.length ? null : { ...command, type: 'vote', targetId: targets[offset(me.seatId + phase.id) % targets.length].id };
    }
    const action = me.action;
    if (phase.kind !== 'night' || phase.nightStage !== 'acting' || !action) return null;
    const act = { ...command, type: 'act', actionId: action.id, targets: [] };
    const targets = action.targets.map(target => target.id);
    if (targets.length < action.min) return action.canSkip ? { ...act, skip: true } : null;
    if (action.roleId === 'vampire' && targets.length) {
        // Every vampire sees these ballots. Follow a human's choice if present;
        // otherwise all bots use one shared choice. Waiting bots must yield.
        const votes = action.votes || [];
        const human = votes.find(vote => targets.includes(vote.targetId) && view.seats.some(seat => seat.id === vote.seatId && !seat.isBot));
        const targetId = human?.targetId || targets[offset(phase.id) % targets.length];
        if (votes.find(vote => vote.seatId === me.seatId)?.targetId === targetId) return null;
        return { ...act, targets: [targetId] };
    }
    if (action.options?.length) {
        act.choice = action.options[offset(action.id) % action.options.length].id;
        if (action.roleId === 'nostradamus') act.choice = targets.length && action.options.some(option => option.id === 'inspect') ? 'inspect' : 'finish';
        if (action.roleId === 'empath') {
            const original = me.originalRoleId || me.roleId;
            const yes = action.questionId === 'viewed' ? me.botActivity?.viewed
                : action.questionId === 'moved' ? me.botActivity?.moved
                : WOLVES.has(original) || VAMPIRES.has(original) || expansion.ALIENS.includes(original) || expansion.VILLAINS.includes(original);
            act.choice = yes ? 'yes' : 'no';
        }
    }
    const count = action.roleId === 'nostradamus' ? Number(act.choice === 'inspect') : Math.min(action.max, targets.length);
    const start = targets.length ? offset(action.id) % targets.length : 0;
    act.targets = [...targets.slice(start), ...targets.slice(0, start)].slice(0, count);
    return act;
}
function performBotDecision(room, now) {
    for (const seat of room.seats) {
        if (!seat.isBot || !seat.actorId || room.members[seat.actorId]?.seatId !== seat.id || seat.actorId === room.hostId) continue;
        const command = chooseBotCommand(publicView(room, seat.actorId, now));
        if (!command) continue;
        applyCommand(room, seat.actorId, command, now);
        room.botState ||= { mode: 'automatic', nextActionAt: 0 };
        // Also set after manual steps, preventing the service's following tick
        // from performing a second decision in the same transaction.
        room.botState.nextActionAt = now + BOT_ACTION_DELAY;
        return true;
    }
    return false;
}
function tickRoom(room, now) {
    now = nowMs(now);
    if (room.status !== 'playing' || room.phase.paused) return false;
    if ((room.botState?.mode || 'automatic') === 'automatic' && now >= (room.botState?.nextActionAt || 0) && performBotDecision(room, now)) return true;
    if (room.phase.kind !== 'night' || room.phase.deadline == null || now < room.phase.deadline) return false;
    if (room.phase.nightStage === 'opening') beginActing(room, now);
    else if (room.phase.nightStage === 'closing') nextTurn(room, now);
    else if (!room.nightActors.length) finishActing(room, now);
    else return false;
    room.revision++; room.updatedAt = now; return true;
}
function recoverHost(room, actor, now) { now = nowMs(now); if (room.status === 'disbanded') fail('ROOM_CLOSED', 'This room has closed.'); const existing = seatOf(room, room.hostId); const own = seatOf(room, actor); if (own && own.id !== existing?.id) fail('RECOVERY_SEATED', 'Use an unseated browser to recover the host.'); const old = room.hostId; if (existing) replace(room, existing, actor, existing.name, now); else { const added = makeSeat('Host', actor); room.seats.push(added); room.members[actor] = { seatId: added.id, lastSeen: now }; } delete room.members[old]; if (existing) room.members[actor] = { seatId: existing.id, lastSeen: now }; room.hostId = actor; room.requests = room.requests.filter(r => r.actorId !== actor); room.revision++; room.updatedAt = now; return room; }
function publicView(room, actor, now) {
    room = clone(room);
    now = nowMs(now); const seat = seatOf(room, actor), isHost = Boolean(room.hostId && actor === room.hostId), finished = room.status === 'finished';
    const publicTokens = finished || ['discussion', 'voting'].includes(room.phase.kind);
    return { code: room.code, gameId: room.gameId || null, revision: room.revision, status: room.status, isHost, hostSeatId: seatOf(room, room.hostId)?.id || null, bots: { mode: room.botState?.mode === 'manual' ? 'manual' : 'automatic', count: room.seats.filter(s => s.isBot).length }, settings: clone(room.settings), roleDeck: [...room.roleDeck], seats: room.seats.map((s, i) => ({ id: s.id, number: i + 1, name: s.name, photo: s.photo, ready: s.ready, connected: connected(room, s, now), occupied: Boolean(s.actorId), isBot: Boolean(s.isBot), isHost: s.actorId === room.hostId, ...(publicTokens ? { shielded: room.shields.includes(s.id), hasArtifact: Boolean(room.artifacts[s.id]) } : {}), ...(room.revealed.includes(s.id) && room.cards[s.id] ? { revealedRoleId: room.cards[s.id].roleId } : {}), ...(finished ? { roleId: room.result?.players.find(p => p.seatId === s.id)?.roleId || effective(room, s.id).roleId } : {}) })), requests: isHost ? room.requests.map(r => ({ id: r.id, name: r.name, createdAt: r.createdAt })) : [], myRequest: room.requests.some(r => r.actorId === actor) ? { status: 'pending' } : null, me: seat ? { seatId: seat.id, roleId: seat.originalRoleId, originalRoleId: seat.originalRoleId, ready: seat.ready, ...(seat.isBot ? { botActivity: { viewed: room.actionLog.some(a => a.seatId === seat.id && a.type === 'view'), moved: room.actionLog.some(a => a.seatId === seat.id && a.type === 'move') } } : {}), knowledge: clone(seat.knowledge), action: descriptor(room, seat), vote: room.votes[seat.id] || null } : null, phase: { ...clone(room.phase), ...(room.phase.kind === 'night' && room.phase.nightStage === 'acting' ? { deadline: null } : {}) }, events: clone(room.events), result: finished ? clone(room.result) : null, pendingVoterIds: room.phase.kind === 'voting' ? room.seats.filter(s => !room.votes[s.id]).map(s => s.id) : [], publicRules: clone(room.expansion?.publicRules || []), centerCards: Object.entries(room.cards).filter(([id]) => id.startsWith('center:')).map(([id, card]) => ({ id, ...(finished || room.revealed.includes(id) ? { roleId: card.roleId } : {}) })), centerCount: Object.keys(room.cards).filter(id => id.startsWith('center:')).length || 3 };
}
export { createRoom, applyCommand, publicView, tickRoom, recoverHost, chooseBotCommand, DEFAULT_SETTINGS, catalogue, validateDeck, defaultDeck,effective as effectiveCard};
